import java.util.*;
import java.util.stream.Collectors;

public class E3V1AddTwoNumbers {
    public static void main(String[] args) {
        int x[] = { 1, 2, 3 };
        int y[] = { 1, 2, 3, 4 };
        List<Integer> a = Arrays.stream(x).boxed().collect(Collectors.toList());
        List<Integer> b = Arrays.stream(y).boxed().collect(Collectors.toList());
        List<Integer> res = add(a, b);
        System.out.println(res);
    }

    public static List<Integer> add(List<Integer> x, List<Integer> y) {
        List<Integer> res = new ArrayList<>();
        int carry = 0;

    }
}